const TabLayout = () => {
/*   const { signUp } = useSignUpContext();
  const { currentTab } = signUp;

  return (
    <div className="tab-layout">
      <div className="tab-layout__tabs">
        <Tab
          label="Personal"
          value="personal"
          selected={currentTab === "personal"}
        />
        <Tab
          label="Address"
          value="address"
          selected={currentTab === "address"}
        />
        <Tab
          label="Contact"
          value="contact"
          selected={currentTab === "contact"}
        />
      </div>
      <div className="tab-layout__content">
        {currentTab === "personal" && <Personal />}
        {currentTab === "address" && <Address />}
        {currentTab === "contact" && <Contact />}
      </div>
    </div>
  ); */
};

export default TabLayout;